import turtle

side_length = 100 # Define the length of the sides of the square

# Draw a square using the predefined length
turtle.forward(side_length)
turtle.left(90)
turtle.forward(side_length)
turtle.left(90)
turtle.forward(side_length)
turtle.left(90)
turtle.forward(side_length)

turtle.done()

COMP104 : Quiz for Week 3

Questions 1-3 show a C++ program that has one bug in it. 
Identify and correct the bug. 

1.	#include <iostream>  
     	using namespace std
     	int main( ){
            cout << "hello world!" << endl;
            return 0;
    	}



2.	include <iostream>  
     	using namespace std;
     	int main( ){
            cout << "hello world!" << endl;
            return 0;
    	}
	


3.	#include <iostream>  
     	using namespace std;
     	int main( ){
            cout << hello world! << endl;
            return 0;
    	}



4.	#include <iostream>  
     	using namespace std;
     	int main( ){
            cout << "hello world!" << endl;
            return 0
    	}



Questions 5-13 show a C++ program that has at least one bug in it. 
Identify and correct the bug. 

5.	// calculate the area of a square
	#include <iostream>
	using namespace std;
	int main( ){
		double width;
		double area;
		// read in the value of the width of the square
		cout << "Enter the width of the square: ";
		cin >> width;
		// calculate the area
		Area = width * width;
		// display the results
		cout << "The area of the square with the width = " << width << " is " << area;
		cout << endl;
		return 0;
	}



6.	calculate the area of a square
	#include <iostream>
	using namespace std;
	int main( ){
		double width;
		double area;
		// read in the value of the width of the square
		cout << "Enter the width of the square: ";
		cin >> width;
		// calculate the area
		area = width * width;
		// display the results
		cout << "The area of the square with the width = " << width << " is " << area;
		cout << endl;
		return 0;
	}      



7.	// calculate the area of a square
	#include <iostream>
	using namespace std;
	int main( ){
		double width;
		double area;
		// read in the value of the width of the square
		cout << "Enter the width of the square: ";
		cin >> width;
		// calculate the area
		area = width * width;
		// display the results
		cout << "The area of the square with the width = " << width << " is " << area;
		cout << endl;
		return;
	}



8.	// calculate the area of a rectangle
	#include <iostream>
	using namespace std;
	int main( ){
		double width;
		double height;
		double area;
		// read in the value of the width of the rectangle
		cout << "Enter the width and height of the rectangle: ";
		cin >> width >> height;
		// calculate the area
		area = width * height;
		// display the results
		cout << "The area of the rectangle with the width = " << width; 
		        << " and height =  " << height << " is " << area;
		cout << endl;
		return 0;
	}    



9.	// calculate the area of a rectangle
	#include <iostream>
	using namespace std;
	int main( ){
		double width;
		double height;
		double area;
		// read in the value of the width of the rectangle
		cout << "Enter the width and height of the rectangle: ";
		cin >> width , height;
		// calculate the area
		area = width * height;
		// display the results
		cout << "The area of the rectangle with the width = " << width 
		        << " and height = " << height << " is " << area;
		cout << endl;
		return 0;
	}    



10.	// calculate the area of a rectangle
	#include <iostream>
	using namespace std;
	int main( ){
		double width;
		double 2height;
		double area;
		// read in the value of the width of the rectangle
		cout << "Enter the width and 2height of the rectangle: ";
		cin >> width >> 2height;		
		cin >> endl;
		// calculate the area
		area = width * 2height;
		// display the results
		cout << "The area of the rectangle with the width = " << width 
		        << " and height = " << 2height << " is " << area;
		cout << endl;
		return 0;
	}    


11.   	// compute area & circumference 
      	// of circle with given radius      
      	#include <iostream>
      	using namespace std;
      	int main( ) {
               	const Pi = 3.14159;
               	double radius, area, circumference;

               	cout << "Circle radius: ";
               	cin >> radius;
               	area = Pi * radius * radius;
               	circumference = Pi * 2 * radius;
               	cout << "Area of circle with radius " << radius
                                 << " is " << area << endl;
               	cout << "Circumference is " << circumference
                       	        << endl;
               	return 0;
      	}



12.		// compute area & circumference 
		// of circle with given radius
		#include <iostream>
        	using namespace std;
        	int main( ) {
               		const double Pi = 3.14159;
               		double radius, area, circumference;

               		cout << "Circle radius: ";
               		cin >> radius;                		
               		area = pi * radius * radius;
               		circumference = pi * 2 * radius;
               		cout << "Area of circle with radius " << radius
                         	        << " is " << area << endl;
               		cout << "Circumference is " << circumference
                        	        << endl;
               		return 0;
        	}


 
13.		// compute area of circle(s)
		#include <iostream>
        	using namespace std;
        	int main() {
                		const double Pi = 3.14159;
                		double radius, single, double, triple;
                		cout << "Circle radius: ";
                		cin >> radius;
                		single = Pi * radius * radius;
                		double = single * single;
                		triple = double * double;
                		return 0;
        	}




Answer 1:	A syntax error.  Missing semicolon after std
Answer 2:	Missing # before include <iostream>
Answer 3:	Needs double quotes around "hello world!"
Answer 4:	A syntax error.   Missing semicolon after return 0
Answer 5:	Undeclared identifier.  area declared, but Area used in assignments
Answer 6:	Comment on first line must start with "//"
Answer 7:	 Command line return should be return 0
Answer 8:	Extra semicolon after "The area of the rectangle with the width = " << width
	or Missing cout befor << " and height  =  " << height << " is " << area;
Answer 9:	Illegal syntax;	cin >> width, height; 
Answer 10:	2height is an illegal identifier name
		cin >> endl  is illegal (delete this line)
Answer 11:	const Pi = 3.14159;	should be:
    		const double Pi = 3.14159;
Answer 12:	Undeclared identifier pi that only Pi is declared
Answer 13:	double is a reserved word


------------------------------------------------------------------------------------

Operator Precedence

Calculate the value of n after each of the following C++ statements is exectued.

1.		int n = 22/8;



2.		int n = 99/8 + 21/11*7;



3.		double a = 4, b = 3, p = 8, q = 2;
		double n = q/a - p/b;



4.		int a = 6, b = 3, p = 8, q = 2;
		double n = p/a + q/b;



5.		int a = 3.3, b = 2.7, p = 8.7 , q = 5.4;
		double n = p/b + q/a;



6.		int n = 11 + 7/4 + 98%6*3;



7.		int n = 11/31%8*5 - 12;



8.		double p = 8;
        	int q = 5;
        	int n = q*p + 3.0*p*p - (q%3)*p*p*p;



9.		double p = 5;
        	int q = 9;
        	int n = (q%2)*p + 6.0*p*(q%3) + (q%4)*p*p*p;





Answer 1:	2
Answer 2:	19
Answer 3:	-2.16667
Answer 4:	1
Answer 5:	5
Answer 6:	18
Answer 7:	-12
Answer 8:	-792
Answer 9:   130

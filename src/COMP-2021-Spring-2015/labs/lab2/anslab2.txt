1. 3) :%s/unix/Unix/g 4) :g/vi/s/emacs/emacs-bad-editor
2. 1) chmod a+r test 2) chmod u=rw,go= test 3) chmod 664 test 
3. 1) yes 2) yes 3) yes 4) no
4. 1) sleep 200 Ctrl-Z bg jobs ps fg
   2) sleep 300 & kill %1
   3) sleep 300 & kill pid
   4) killall Firefox
5. grep SECRET enfile | sed 's/[a-z]*//g' | grep -o 'M[0-9]\+' | sed 's/M//g'
